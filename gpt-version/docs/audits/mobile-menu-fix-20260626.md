# Mobile Menu Fix 2026-06-26

## Сделано

- Добавлен нормальный главный экран `Главное меню`.
- Добавлены быстрые действия:
  - Принять товар;
  - Проверить остаток;
  - Начать проверку;
  - Печать этикеток.
- Добавлены карточки разделов приложения.
- На mobile добавлена нижняя навигация:
  - Меню;
  - Товары;
  - Склад;
  - График;
  - Еще.
- Mobile drawer получил backdrop и кнопку закрытия.
- Dev/test controls скрыты из мобильного topbar.
- Убран page-level horizontal overflow на проверенных основных экранах.
- Таблицы `DataTable` на mobile переведены в карточки.
- Форма товара скрыта до явного действия `Создать товар` / `Править`.
- Форма складского движения скрыта по умолчанию; быстрое действие `Принять товар` открывает ее сразу в режиме прихода.
- `Проверить остаток` открывает склад без формы, сразу со списком остатков.
- Браузерные `confirm` заменены на встроенный экран подтверждения.
- Пользовательские тексты очищены от `Snapshot`, `merge`, `backup`, `commit`.
- Форма импорта скрыта по умолчанию и открывается только по `Новый импорт`.
- Формы графика (`Новая смена`, `Обмен смены`, `Копирование смены`) вынесены в отдельные действия.

## Browser-проверка

Viewport после reset/set: фактическая ширина `335px`.

| Экран | scrollWidth | clientWidth | overflow | topbar |
|---|---:|---:|---|---:|
| Главное меню | 335 | 335 | нет | 61px |
| Товары | 335 | 335 | нет | 61px |
| Склад | 335 | 335 | нет | 61px |
| График | 335 | 335 | нет | 61px |

Дополнительно:

- `Товары`: строка таблицы рендерится карточкой, `rowDisplay=block`, `rowWidth=277`, форма создания по умолчанию скрыта.
- `Создать товар`: открывает форму без horizontal overflow.
- `Склад`: строка таблицы рендерится карточкой, `rowDisplay=block`, `rowWidth=277`.
- `Проверить остаток`: форма движения скрыта, блок остатков `307px`, overflow нет.
- `Принять товар`: форма движения открыта, тип операции `receipt`, overflow нет.
- `Товары -> Архив`: открывает встроенный confirm-dialog, отмена закрывает его, overflow нет.
- `Импорт`: форма нового импорта скрыта по умолчанию, история видима, overflow нет.
- `Новый импорт`: открывает форму импорта без overflow.
- `График`: формы смен и обмена скрыты по умолчанию, календарь видим, overflow нет.
- `Новая смена`: открывает форму смены без overflow.

## Interaction

- `Принять товар` открывает `Склад` сразу с формой прихода.
- `Проверить остаток` открывает `Склад` сразу со списком остатков.
- Нижняя кнопка `Меню` возвращает на главный экран.
- Нижняя кнопка `Еще` открывает drawer.
- Drawer показывает backdrop и кнопку закрытия.

## Проверки

- `npm run check`
- `npm run test`
- `npm run build`

## Осталось

- Критичного UX-долга по mobile-сценариям не осталось.
