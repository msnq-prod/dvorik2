# ⚡ Quick Start Guide

Запуск проекта за 5 минут!

## 🎯 Prerequisites

- Docker & Docker Compose
- Telegram bot tokens (2 бота)
- Your Telegram ID

## 📋 Step-by-Step

### 1. Получить Telegram токены (2 минуты)

```
1. Открыть @BotFather в Telegram
2. Создать главного бота:
   /newbot
   Name: Мармеладный Дворик
   Username: your_bot_name_bot
   → Сохранить TOKEN
   
3. Создать бота-кассира:
   /newbot
   Name: Мармеладный Дворик Кассир
   Username: your_cashier_bot_name_bot
   → Сохранить TOKEN
   
4. Узнать свой Telegram ID:
   Открыть @userinfobot
   → Сохранить ID
```

### 2. Настроить проект (1 минута)

```bash
# Использовать Makefile (рекомендуется)
make setup

# Или вручную:
cp .env.example .env

# Сгенерировать секретные ключи
openssl rand -hex 32  # JWT_SECRET_KEY
openssl rand -hex 32  # INTERNAL_API_KEY
openssl rand -hex 32  # TELEGRAM_WEBHOOK_SECRET
```

### 3. Отредактировать .env

```bash
nano .env

# Заполнить обязательные поля:
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_AUTH_BOT_TOKEN=789012:GHI-JKL...
SUPERADMIN_TELEGRAM_ID=123456789
JWT_SECRET_KEY=<сгенерированный ключ>
INTERNAL_API_KEY=<сгенерированный ключ>
TELEGRAM_WEBHOOK_SECRET=<сгенерированный ключ>
```

### 4. Запустить проект (2 минуты)

```bash
# Использовать Makefile
make start

# Подождать пока запустится (30 сек)
make migrate
make webhooks

# Или использовать скрипт
./scripts/setup.sh

# Или вручную
docker-compose up -d
sleep 30
docker-compose exec api alembic upgrade head
```

### 5. Проверить работу

```bash
# Проверить API
curl http://localhost:8000/health

# Или
make test-health

# Открыть документацию
make docs
# или открыть: http://localhost:8000/api/docs
```

## ✅ Готово!

Теперь можно:

1. **Протестировать главного бота:**
   - Найти бота в Telegram
   - Нажать /start
   - Нажать "Подписаться" → получить скидку

2. **Протестировать бота-кассира:**
   - Найти бота в Telegram  
   - Нажать /start (от админского аккаунта)
   - Получить ссылку для входа в админку

3. **Изучить API:**
   - http://localhost:8000/api/docs

## 🔧 Полезные команды

```bash
# Просмотр логов
make logs              # Все логи
make logs SERVICE=api  # Только API

# Проверка статуса
make status

# Остановка
make stop

# Перезапуск
make restart

# Тесты
make test

# Помощь
make help
```

## 🐛 Troubleshooting

### API не запускается
```bash
make logs SERVICE=api
# Проверить .env файл
cat .env | grep TELEGRAM
```

### Webhooks не работают
```bash
make webhooks-info
# Переустановить
make webhooks
```

### База данных
```bash
# Проверить миграции
make migrate
# Открыть MySQL shell
make db
```

## 📚 Дальше

После успешного запуска:

1. Прочитать **API_EXAMPLES.md** - примеры всех запросов
2. Прочитать **DEPLOYMENT.md** - production деплой
3. Изучить **PROJECT_SUMMARY.md** - детали проекта

## 🎉 Успехов!

При проблемах:
- Проверить `make logs`
- Проверить `.env` файл
- Посмотреть документацию

**Проект готов к работе!** 🍬

