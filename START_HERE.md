# 🍬 Мармеладный Дворик - START HERE

## 🎉 Проект готов! Начните отсюда:

### 📋 Что сделано

✅ **78 Python модулей** (~14,000 строк кода)  
✅ **10 Database tables** с полным набором индексов  
✅ **50+ REST API endpoints** с документацией  
✅ **2 Telegram Bot handlers** (main + auth)  
✅ **10+ Celery background tasks**  
✅ **78 Pydantic schemas** с валидацией  
✅ **47 Machine-readable error codes**  
✅ **Docker Compose** конфигурация  
✅ **Полная документация**

---

## 🚀 Быстрый старт за 5 минут

### Шаг 1: Получить токены Telegram

```bash
# 1. Открыть @BotFather в Telegram
# 2. Создать 2 бота:
#    - /newbot → "Мармеладный Дворик" → получить MAIN_BOT_TOKEN
#    - /newbot → "Мармеладный Дворик Кассир" → получить AUTH_BOT_TOKEN
# 3. Узнать свой Telegram ID через @userinfobot
```

### Шаг 2: Настроить .env

```bash
# Скопировать шаблон
cp .env.example .env

# Отредактировать .env:
nano .env

# Заполнить обязательные поля:
# - TELEGRAM_BOT_TOKEN=<ваш токен main бота>
# - TELEGRAM_AUTH_BOT_TOKEN=<ваш токен auth бота>
# - SUPERADMIN_TELEGRAM_ID=<ваш Telegram ID>
# - JWT_SECRET_KEY=<openssl rand -hex 32>
# - INTERNAL_API_KEY=<openssl rand -hex 32>
# - TELEGRAM_WEBHOOK_SECRET=<openssl rand -hex 32>
```

### Шаг 3: Запустить все сервисы

```bash
# Запустить Docker Compose
docker-compose up -d

# Подождать ~30 секунд пока все запустится
docker-compose ps

# Должны быть running:
# - mysql
# - redis  
# - api
# - worker
# - beat
# - flower
```

### Шаг 4: Применить миграции

```bash
# Создать таблицы и начальные данные
docker-compose exec api alembic upgrade head

# Проверить
docker-compose exec api alembic current
# Должно показать: 003_add_composite_indexes (head)
```

### Шаг 5: Настроить webhooks

```bash
# Получить INTERNAL_API_KEY из .env
cat .env | grep INTERNAL_API_KEY

# Установить webhooks (замените YOUR_KEY)
curl -X POST http://localhost:8000/internal/set-webhooks \
  -H "X-API-Key: YOUR_INTERNAL_API_KEY"

# Должен вернуть: {"success": true, ...}
```

### Шаг 6: Проверить работу

```bash
# Проверить API
curl http://localhost:8000/health
# {"status":"healthy","environment":"development"}

# Открыть документацию
open http://localhost:8000/api/docs

# Открыть Flower (мониторинг Celery)
open http://localhost:5555

# Проверить логи
docker-compose logs -f api
```

### Шаг 7: Протестировать ботов

```bash
# 1. Открыть главного бота в Telegram
# 2. Нажать /start
# 3. Должно прийти приветствие с кнопками
# 4. Нажать "Подписаться" → получить скидку

# 5. Открыть бота-кассира
# 6. Нажать /start (от админского аккаунта)
# 7. Должна прийти ссылка для входа в админку
```

---

## 📚 Полная документация

- **README.md** - обзор проекта, архитектура, API
- **DEPLOYMENT.md** - развертывание в production
- **PROJECT_SUMMARY.md** - детальная сводка проекта
- **TASKS.md** - полный план разработки (14 фаз)

---

## 🔧 Полезные команды

### Docker

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск одного сервиса
docker-compose restart api

# Логи
docker-compose logs -f
docker-compose logs -f api
docker-compose logs -f worker

# Статус
docker-compose ps

# Пересборка после изменений кода
docker-compose build
docker-compose up -d
```

### Database

```bash
# Применить миграции
docker-compose exec api alembic upgrade head

# Откатить миграцию
docker-compose exec api alembic downgrade -1

# Текущая версия
docker-compose exec api alembic current

# История миграций
docker-compose exec api alembic history

# Подключиться к MySQL
docker-compose exec mysql mysql -u dvorik_user -p dvorik_db
```

### Celery

```bash
# Список задач
docker-compose exec worker celery -A core.celery_app inspect active

# Статистика
docker-compose exec worker celery -A core.celery_app inspect stats

# Очистить очередь
docker-compose exec redis redis-cli del celery
```

### Тестирование API

```bash
# Health check
curl http://localhost:8000/health

# Webhook info
curl -X GET http://localhost:8000/internal/webhook-info \
  -H "X-API-Key: YOUR_KEY"

# Статистика (нужен JWT токен)
curl http://localhost:8000/api/v1/stats/kpi?days=30
```

---

## 🎯 Основные фичи

### 1. Main Bot (для клиентов)

**Команды:**
- `/start` - регистрация, приветствие
- `/start ref_instagram` - регистрация с отслеживанием источника

**Кнопки:**
- 🔔 Подписаться → выдача скидки
- 🎁 Мои скидки → список активных скидок
- ℹ️ Помощь

**Автоматика:**
- Скидка при подписке (если настроено)
- Скидка в день рождения (09:00 каждый день)
- Уведомление о погашении скидки

### 2. Auth Bot (для кассиров и админов)

**Для кассиров:**
- `/start` - регистрация (ожидание одобрения)
- Отправить код → валидация скидки
- Кнопка "Погасить" → погашение скидки

**Для админов:**
- `/start` → получение ссылки для входа в админку
- One-time token → обмен на JWT

### 3. Admin Panel API

**Эндпоинты:**
- Users: просмотр, фильтрация, массовые операции
- Discounts: просмотр, выдача, статистика
- Broadcasts: создание, планирование, отправка
- Segments: создание фильтров пользователей
- Settings: управление настройками системы
- Stats: KPI, детальная статистика

---

## 🐛 Проблемы и решения

### API не запускается

```bash
# Проверить логи
docker-compose logs api

# Часто: не заполнены переменные в .env
cat .env | grep TELEGRAM_BOT_TOKEN
```

### Миграции не применяются

```bash
# Проверить подключение к БД
docker-compose exec api python -c "from core.config import settings; print(settings.DATABASE_URL)"

# Применить принудительно
docker-compose exec api alembic upgrade head --sql
docker-compose exec api alembic upgrade head
```

### Webhooks не работают

```bash
# Проверить, что установлены
curl -X GET http://localhost:8000/internal/webhook-info \
  -H "X-API-Key: YOUR_KEY"

# Переустановить
curl -X POST http://localhost:8000/internal/set-webhooks \
  -H "X-API-Key: YOUR_KEY"
```

### Celery задачи не выполняются

```bash
# Проверить worker
docker-compose logs worker

# Проверить beat
docker-compose logs beat

# Проверить Redis
docker-compose exec redis redis-cli ping
# Должно вернуть: PONG
```

---

## 🎓 Следующие шаги

### Для разработки

1. **Создать Frontend админ-панель** (React + TypeScript)
2. **Добавить тесты** (pytest)
3. **Настроить CI/CD** (GitHub Actions)
4. **Добавить мониторинг** (Prometheus + Grafana)

### Для production

1. **Развернуть на сервере** (см. DEPLOYMENT.md)
2. **Настроить Nginx** с SSL
3. **Настроить backup** БД
4. **Настроить мониторинг и алерты**

---

## 📞 Поддержка

- **Документация:** README.md, DEPLOYMENT.md
- **API Docs:** http://localhost:8000/api/docs
- **Flower:** http://localhost:5555

---

## ✨ Возможности системы

✅ Автоматическая выдача скидок  
✅ Отслеживание источников клиентов  
✅ Сегментация пользователей  
✅ Запланированные рассылки  
✅ Статистика и KPI  
✅ Аудит всех действий  
✅ RBAC для админов  
✅ Полная machine-readability  

**Проект готов к работе! 🎉**

