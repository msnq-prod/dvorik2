# A02 Solution Plan

1. Отметить mock-only экраны прямо в current-state docs.
2. Перед разработкой выбрать целевой UI: текущий sidebar или `gpt-version` admin shell.
3. Ввести auth/session source of truth до role-based UI.
4. Подключать страницы к API по одному домену, не все сразу.

