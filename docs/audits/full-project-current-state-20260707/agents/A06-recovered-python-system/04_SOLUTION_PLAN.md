# A06 Solution Plan

1. Использовать `recovered-dvorik` как legacy/reference, не как целевой код.
2. Извлечь бизнес-правила: imports, no-negative-stock, schedule swaps, notifications.
3. Сравнить с `gpt-version`; выбрать более чистую модель для переноса.
4. Не смешивать Python runtime с root без отдельного решения.

