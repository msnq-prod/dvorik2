# A05 Needs More Research

- Запустить checks из `gpt-version/README.md`.
- Снять route map из `/api/openapi.json`.
- Сопоставить `gpt-version` types с `docs/crm/data-model.md`.
- Проверить тесты `gpt-version/src/server/domain.test.ts`.

