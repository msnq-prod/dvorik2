# FIX-507 — Tablet buffer для разрешённых ролей

- Статус: `pending`; P1; зависит от SEC-303.
- Контуры: role/route matrix, `StockPage`, buffer API, browser tests.

## Шаги
1. Удалить admin-only structural return.
2. Рассчитывать доступ отдельно для transfer/write-off/inventory.
3. UI скрывает/объясняет запрет; backend независимо проверяет каждую entry.
4. Seller/admin/inventory-only fixtures.

## DoD
Разрешённый seller использует buffer; direct forbidden entry даёт 403 без partial
apply; UI и API matrix совпадают.
