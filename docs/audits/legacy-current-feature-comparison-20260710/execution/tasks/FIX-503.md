# FIX-503 — Согласованный media limit

- Статус: `pending`; P0; зависимости: нет; временный bridge до MED-1101.
- Контуры: Express parsers/upload route/UI copy/tests.

## Шаги
1. Измерить decoded/base64/proxy limits и выбрать временный точный limit.
2. Endpoint-specific parser/413 error; не повышать глобально body limit без нужды.
3. UI показывает тот же limit; записать migration к multipart.

## Negative/DoD
Файл ровно limit принимается, +1 byte/oversized base64 отклоняются до decode/write,
ошибка не 500 и временное решение не объявляется streaming production upload.
