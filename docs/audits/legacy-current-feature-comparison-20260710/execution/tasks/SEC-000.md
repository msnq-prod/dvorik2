# SEC-000 — Закрыть dev/demo endpoints

- Статус: `pending`; P0; зависит от SEC-001.
- Контуры: `src/server/index.ts`, auth/route tests, OpenAPI/dev docs.

## Шаги
1. Найти demo/dev routes и все обходные entrypoints.
2. Регистрировать только при `DVORIK_DEV_TOOLS=1` и безопасной dev policy.
3. В production отвечать `404`; не полагаться только на `NODE_ENV`.
4. Удалить dev routes из production OpenAPI; не сломать isolated test harness.

## Negative/DoD
Известный `u-super` не создаёт production session; direct URL/alternate method тоже
закрыты; dev test работает только с флагом. Focused auth contract + full gate.
