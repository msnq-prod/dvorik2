# FIX-501 — Единый movements report DTO

- Статус: `pending`; P0; зависимости: нет.
- Контуры: `reports.ts`, report route/OpenAPI, `ReportsPage.tsx`, contract/E2E tests.

## Шаги
1. Зафиксировать один DTO и date/product/location field names.
2. Удалить unsafe casts и использовать DTO в JSON/CSV/PDF/UI.
3. Добавить runtime UI regression с реальной movement row и empty state.

## Negative/DoD
Invalid/missing legacy metadata обрабатывается явно; `createdAt.slice` на undefined
невозможен; JSON→UI contract test, check/test/build/browser smoke green.
