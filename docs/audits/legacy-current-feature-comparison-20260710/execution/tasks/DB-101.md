# DB-101 — In-process SQLite driver

- Статус: `pending`; P0; зависит от SEC-001.
- Контуры: package/deployment, новый database adapter, store/migrate/tests.

## Шаги
1. Сравнить поддерживаемые drivers на target architectures и зафиксировать решение.
2. Реализовать open/query/execute/transaction/close без sqlite subprocess.
3. Test DB path isolated; errors получают typed context без SQL secrets.
4. Обновить build/deploy/runbook.

## DoD
Нет runtime `spawnSync(sqlite3)`; driver работает в production artifact и tests;
focused open/query/transaction/error checks green.
