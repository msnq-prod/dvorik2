# FIX-504 — Обязательное production-сжатие

- Статус: `pending`; P0; зависит от SEC-001.
- Контуры: dependencies, `media.ts`, config/startup, media tests/build target.

## Шаги
1. Подключить поддерживаемый processor и проверить target architecture.
2. Удалить silent production fallback; dev exception только explicit.
3. Fixture проверяет decode/resize/dimensions/bytes, не только boolean.
4. Ошибка processor не сохраняет original как success.

## DoD
Production startup знает capability; большой fixture реально уменьшается; metadata/
orientation policy документированы для MED-1102.
