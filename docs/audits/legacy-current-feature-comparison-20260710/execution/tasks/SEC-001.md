# SEC-001 — Fail-fast production config

- Статус: `pending`; P0; зависит от DOC-002.
- Контуры: новый config module, startup, workers, tests, runbook.

## Шаги
1. Реестр env: required/default/sensitive/owner для DB/TZ/cookie/Telegram/storage/backup/release.
2. Валидировать до listen/worker loop; запретить dev secrets/JSON/local fallback.
3. Проверить права DB/media/backup paths без бизнес-записи.
4. Логировать только факт наличия, не значения secrets.

## Negative/DoD
Каждый missing/unsafe параметр завершает production startup non-zero; test/dev
adapter включается только явно. API и workers используют один validator.
