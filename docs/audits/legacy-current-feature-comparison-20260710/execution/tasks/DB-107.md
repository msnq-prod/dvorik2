# DB-107 — SQL read/query paths

## Статус

- `done`; P0; зависимости `DB-105`, `DB-106` выполнены.

## Цель

Dashboard, catalog/search, reports, schedule/day detail, inventory snapshot,
audit/outbox reads получают данные из SQLite repositories с visibility до fetch,
stable order и pagination. Production endpoint не читает global `db`.

## Evidence criteria

- каждый release-critical read route traced UI → API → query repository;
- direct route permissions/visibility negative;
- stable pagination/order and empty/corrupt-row negative;
- `rg` подтверждает отсутствие `db.` в принятых query routes;
- focused and block gate pass.

## Scope

Только release-critical production read paths. Deferred imports/merge/archive,
Telegram expanded commands и secondary report types остаются disabled.

## Выполнение

- Evidence: `../evidence/DB-107.md`.
- Production SQL adapter покрывает dashboard/catalog/search/stock/inventory/
  reports/schedule/audit; two-process visibility и negative gates зелёные.
- Следующий ID: `DB-108`.
