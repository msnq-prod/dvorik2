# DB-106 — SQLite mappers

- Статус: `pending`; P0; зависит от DB-103, DB-105.
- Контуры: все normalized tables, mapper tests, migration compatibility.

## Шаги
1. Явно map enum/NULL/JSON/quantity/UTC timestamp/local date/version.
2. Не смешивать SQLite datetime и ISO Z; local date не прогонять через UTC.
3. Corrupt JSON/unknown enum дают entity-specific error.
4. Round-trip fixtures current entities и nullable/legacy edges.

## DoD
Все entity round-trip tests green; mapper не подменяет corrupt data default-ом;
conversion invariants совпадают.

## Mapper checkpoint 2026-07-12

- Каждая normalized table имеет explicit `fromRow` и `toRow`; aggregate repository собирается выше.
- Shared mapper core владеет enum/NULL/integer/versioned JSON/UTC/local date/quantity semantics.
- Mapper error всегда содержит entity/table, entity ID, field и stable code; corrupt JSON не заменяется.
- JSON envelope поддерживает schema version `1`; legacy raw JSON явно оборачивается как v1 compatibility.
- UTC возвращает ISO `Z`; legacy SQLite UTC без offset разрешён только как explicit compatibility input.
- Local date валидируется календарно без timezone conversion.
- Quantity читается из minor integer; transitional REAL обязан совпасть.
