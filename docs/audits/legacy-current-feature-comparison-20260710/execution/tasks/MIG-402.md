# MIG-402 — Runtime schema extension

## Статус и границы
- Статус: `in_progress`; P0; зависит от `DB-105`.
- Входит: runtime columns/tables для repository/mappers.
- Не входит: repository implementations, service cutover, production migration.

## Шаги
1. Добавить Telegram update inbox, WebApp notifications, migration provenance.
2. Добавить session token hash и missing version/updated timestamps.
3. Добавить shift times/version coverage и workflow revisions.
4. Добавить outbox max attempts, lease owner/token/expiry, error code, failed timestamp, version.
5. Обновить required-table readiness, migration/state fixtures и mapper actual-schema smoke.

## Negative/DoD
- Re-run migration idempotent; existing rows сохранены.
- Invalid lease/version/max-attempt values rejected constraints.
- Fresh DB и upgrade v3→v4 имеют одинаковую schema.
- Mapper integration читает actual rows без compatibility `app_state`.
- Focused migration/schema tests и `npm run check && npm run test && npm run build` pass.
