# FIX-506 — Receipt по существующему supplier SKU

- Статус: `pending`; P0; зависит от TX-208.
- Контуры: supplier/SKU repositories, import preview/commit/UI/tests.

## Шаги
1. Нормализовать supplier identity и SKU по утверждённым правилам.
2. Exact `(supplierId, normalizedSku)` → `receipt_existing`; no match → create.
3. Multiple/conflict → rejected/needs resolution, не arbitrary first match.
4. Commit повторно проверяет mapping в transaction.

## DoD
Повторная поставка не создаёт product duplicate; одинаковый SKU другого supplier
допустим; preview/commit/audit/evidence показывают выбранный product.
