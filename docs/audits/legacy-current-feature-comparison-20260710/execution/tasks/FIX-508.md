# FIX-508 — Inventory snapshot version в buffer

- Статус: `pending`; P0; зависит от TX-204, TX-205.
- Контуры: draft schema, buffer projection/API, inventory service, E2E.

## Шаги
1. При подсчёте сохранить expected+version, не перечитывать их при apply.
2. Commit сверяет saved version и возвращает row-specific 409.
3. Failed entry остаётся редактируемой; хвост not_run; resume stable key.

## DoD
Concurrent stock movement между count/apply обнаруживается; ни одна inventory
adjustment не рассчитывается на silently refreshed snapshot.
