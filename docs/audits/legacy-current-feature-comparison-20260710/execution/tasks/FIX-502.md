# FIX-502 — Privacy чужих смен продавца

- Статус: `pending`; P0; зависимости: нет.
- Контуры: schedule query/route, `SchedulePage.tsx`, role E2E.

## Шаги
1. Найти server/UI fallback и все swap selectors.
2. Seller query возвращает только own shifts/swaps; manager сохраняет team view.
3. Удалить client fallback, показать empty state.
4. Проверить direct API и UI, не только скрытие option.

## Negative/DoD
Seller без shifts не видит чужие IDs/names/location/comments и не может создать
swap на чужую смену; admin flow не сломан.
