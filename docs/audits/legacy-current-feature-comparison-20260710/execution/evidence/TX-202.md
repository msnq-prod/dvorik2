# TX-202 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: canonical hash, SQLite idempotency repository и UoW execution; endpoint wiring входит в следующие TX IDs.

## Изменения

- Recursive canonical JSON + SHA-256; invalid/non-deterministic runtime values отклоняются typed `400`.
- Atomic insert-first claim, exact response/status replay, different-payload `409`, active `processing` и timeout recovery.
- Conditional terminal update использует hash/status/revision; stale claimant не может завершить recovered claim.
- `failed` — terminal replayable HTTP response; thrown/transient fault откатывает весь UoW.

## Проверки

| Проверка | Результат |
| --- | --- |
| reordered nested JSON / quantity difference / invalid JSON | pass |
| success + failed exact status/body replay | pass |
| active/expired/NULL-deadline legacy recovery boundary | pass |
| stale completion, fault rollback, terminal-only cleanup | pass |
| two overlapping workers, independent SQLite connections | one mutation + replay/conflict |
| corrupt response/timestamp rows | rejected |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent final review: `accepted`; reportable findings none.
- Worker threads use independent connections and synchronized overlapping writes; OS-process rehearsal remains later TST/REL scope.
- Production cutover remains prohibited; registry status `done`.
