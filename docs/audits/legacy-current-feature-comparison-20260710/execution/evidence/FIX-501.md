# FIX-501 — evidence

- Дата/время: 2026-07-11, Asia/Vladivostok.
- Исполнитель/reviewer: ведущий агент; read-only API trace subagent reviewed the server contract.
- Worktree: `gpt-version` untracked; no production deployment/cutover.

## Изменения

- `src/shared/types.ts`: canonical `MovementReportRow`.
- `src/server/reports.ts`, `src/server/index.ts`: JSON/CSV/PDF use one ordered DTO; product/location filters apply to movements; missing legacy fields become explicit nullable/fallback values.
- `src/client/src/pages/ReportsPage.tsx`: movement renderer uses DTO and no `createdAt.slice` on an undefined legacy field; separate typed states remove report casts.
- `src/server/reports.test.ts`, `src/client/src/pages/ReportsPage.test.tsx`: real-row, corrupt legacy row and empty-state regressions.
- `package.json`, `PROJECT_STATE.md`, OpenAPI summary: test and contract documentation synchronized.

## Автоматические проверки

| Команда | Результат | Факт |
|---|---|---|
| `npm run check` | pass | 2026-07-11 |
| `npm test` | pass | domain, reports contract, reports page empty/real row, migrations, converter, media |
| `npm run build` | pass | Vite client + esbuild server |

`npm test` выполнялся вне sandbox: sandbox блокирует временный IPC socket `tsx`.

## UI и негативные сценарии

| Сценарий | Результат |
|---|---|
| Local Reports → «Движения» с реальной movement row | pass: таблица показывает дату, тип, товар, количество и основание; console errors/warnings отсутствуют |
| DTO contract | pass: ordered canonical fields, product/location filtering и display names проверены |
| Missing legacy fields | pass: corrupt row не вызывает `slice`; получает explicit `null`/fallback labels |
| Empty movements UI | pass: component regression рендерит «Движений нет» |

Browser evidence: `http://localhost:5177/`, desktop in-app Browser; interaction
Reports → «Движения», screenshot/DOM confirmed table and no framework overlay.
Final state split removed casts without changing rendered table; it is covered by
the final component regression plus final build.

## Риски

- Date bounds validation and CSV formula escaping are outside this atomic ID and
  remain in report hardening `RPT-901`/`RPT-904`.
- Runtime persistence remains transitional; no production cutover.

## Review

- Решение: `accepted`.
- Статус реестра обновлён: `yes`.
- Follow-up: `FIX-502`.
