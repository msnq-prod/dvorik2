# TX-204 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: inventory/reversal SQLite services; runtime endpoint cutover остаётся DB-108/109 scope.

## Изменения

- Inventory проверяет весь batch, duplicate keys, products/locations, actual/expected/version до первой записи.
- Changed rows сохраняют signed discrepancy metadata; cumulative product transition создаётся один раз.
- Reversal re-read валидирует original shape, signed inventory facts, current balances и existing reversal.
- Receipt/writeoff/transfer и ±inventory отменяются корректно; merge/reversal/repeat/corrupt state отклоняются.
- Shared stock event writer сохраняет explicit preferences, correlation и transactional inbox/outbox.

## Проверки

| Проверка | Результат |
| --- | --- |
| mixed invalid/stale/unchanged/duplicate inventory rows | full reject |
| multi-row discrepancy metadata + one aggregate event | pass |
| receipt/writeoff/transfer/±inventory reversal | pass |
| corrupt metadata/repeat/merge/reversal/insufficient | rejected |
| balance/operation/notification/outbox/audit/completion faults | full rollback |
| two inventories from one snapshot | one success, one conflict |
| concurrent different/same-key reversals | one success / exact replay |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent final review: `accepted`; reportable findings none.
- Endpoint remains legacy until DB-108/109 to prevent global AppState projection overwrite.
- Production cutover remains prohibited; registry status `done`.
