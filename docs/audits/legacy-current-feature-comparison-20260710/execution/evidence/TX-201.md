# TX-201 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: shared typed command context и UoW boundary; ingress wiring входит в последующие TX/SEC IDs.

## Изменения

- Context содержит trusted actor, request/correlation ID, channel, idempotency key, injected clock и transaction-scoped repositories.
- Actor получается только через injected resolver; direct/header reference и cross-channel actor отклоняются.
- Web/Telegram user и registered worker system semantics разделены; raw DB context не раскрывает.
- UTC clock проверяет canonical calendar value без silent date normalization.

## Проверки

| Проверка | Результат |
| --- | --- |
| web/Telegram/worker actor + same correlation | pass |
| arbitrary header/cross-channel/bad actor negatives | rejected with `BAD_ACTOR` |
| audit+outbox rollback in one UoW | pass |
| nested service, async and escaped context | pass/rejected as required |
| fixed/moving clock + invalid calendar UTC | pass |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent final review: `accepted`; reportable findings none.
- Residual: production ingress resolvers must be backed by verified session/update/worker registry in later integration IDs.
- Production cutover remains prohibited; registry status `done`.
