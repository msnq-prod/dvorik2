# TX-203 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: transactional stock service + SQLite repository slice; runtime route cutover отложен до DB-108/109.

## Изменения

- `BEGIN IMMEDIATE` mode проведён через Database → UoW → CommandExecutor.
- Реализованы SQLite authorization/product/location/balance/operation/audit/notification/outbox repositories.
- Stock service выполняет permission, canonical idempotency, validation, exact CAS balances, operation/audit/events/response в одном UoW.
- Product-wide zero/threshold events используют before→after, explicit instant preferences и correlation payload.
- Migration 005 добавляет unique reversal link; raw operation idempotency key namespaced.

## Проверки

| Проверка | Результат |
| --- | --- |
| active permission, product/location/shape/quantity | pass |
| missing/create and existing/update exact CAS outcomes | pass |
| same key replay / changed quantity conflict | pass |
| threshold/zero aggregate transitions + preferences | pass |
| faults after sum/balance/operation/audit/notification/outbox/completion | full rollback |
| result overflow vs infrastructure fault | typed `409` / rollback |
| overlapping independent SQLite writers | no lost update, no negative, one replay |
| reversal link duplicate | DB rejected |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent final review: `accepted`; reportable findings none.
- Endpoint intentionally remains legacy until DB-108/109: current global `AppState.saveState()` can overwrite normalized rows.
- Production cutover remains prohibited; registry status `done`.
