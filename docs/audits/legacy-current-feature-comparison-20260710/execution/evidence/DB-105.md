# DB-105 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: domain repository interfaces/fake-service contract; SQLite mappers/implementations — `DB-106+`.
- Production cutover: не выполнялся.

## Изменения

- `src/server/repositories.ts`: 15 repository groups и `ApplicationRepositories` bundle.
- Contract: create/update outcomes split; explicit revision; not-found=`undefined`; versioned JSON;
  bounded branded page limit + explicit order/cursor; nullable system audit actor.
- Idempotency reserve атомарно различает reserved/replay/conflict с mandatory existing hash.
- Outbox settlement требует lease owner/token/revision; claimed type содержит required lease fields.
- Single-role API явно возвращает `ambiguous` при multiple assignments.
- Alias и rotation-template surfaces включены; service-facing context не экспонирует DB.

## Проверки

| Проверка | Результат |
| --- | --- |
| fake product service/repository | found/missing/duplicate/stale/update pass |
| page request | `1..100` integer accepted; `0/101/1.5` rejected |
| idempotency | same hash=`replay`; different hash=`conflict` |
| outbox lease | owner/token/revision match accepted; stale worker/token rejected |
| SQL/driver naming scan | no SQLite SQL/column/driver leakage in contracts |
| focused test + `npm run check` | pass |
| `npm run check && npm run test && npm run build` | pass |

## Review и риски

- Independent review wave 1: `changes_requested` по revision/idempotency/lease/page/roles/null/coverage/raw DB.
- Все findings закрыты; wave 2/final: `accepted`, blockers none.
- Global `PageOrder` требует mapper/repository validation допустимых order per entity.
- Schema/DTO gaps (notifications, shift time/revision, entity revisions, lease/DLQ) не скрыты;
  follow-up: `DB-106`, `MIG-402`, `TX-*`, `OUT-*`.
- Registry status: `done`.
