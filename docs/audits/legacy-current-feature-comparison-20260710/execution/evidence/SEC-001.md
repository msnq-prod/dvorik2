# SEC-001 — evidence

- Дата: 2026-07-11, Asia/Vladivostok.
- Scope: fail-fast configuration only; production cutover не выполнялся.

## Изменения

- `src/server/config.ts`: side-effect-free central validator для production и explicit dev/test defaults.
- `src/server/store.ts`: validates before `db` initialization; production rejects JSON persistence and sqlite fallback.
- `src/server/index.ts`, `telegram-worker.ts`, `media.ts`: use shared config; production requires object storage and cannot silently write local media.
- `src/server/config.test.ts`, `package.json`, runbook/project/execution context updated.

## Проверки

| Проверка | Результат |
| --- | --- |
| `npm run check` | pass |
| focused `config.test.ts` | pass; missing/unsafe timezone, release, secrets, object storage, dirs, cookie, JSON state rejected |
| `npm test` | pass |
| `npm run build` | pass |
| `NODE_ENV=production npx tsx src/server/index.ts` без env | exit `1` on missing `DVORIK_TIMEZONE`; stack ends in config/store before `app.listen` |

## Риски

- Runtime всё ещё использует transitional `AppState`; этот ID не делает SQLite runtime source of truth.
- Production release остаётся запрещён до всех plan no-go, включая отдельный release approval personal-project owner.
