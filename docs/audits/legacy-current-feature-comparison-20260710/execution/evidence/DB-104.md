# DB-104 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: sync Unit of Work foundation; runtime service/repository migration не входит (`DB-105+`, `TX-*`).
- Production cutover: не выполнялся.

## Изменения

- `src/server/unit-of-work.ts`: generic transaction-scoped repository factory/context.
- `src/server/database.ts`: scoped/expiring context; nested, manual transaction-control,
  savepoint, async, close/script-in-transaction guards; rejected async quarantine.
- `src/server/unit-of-work.test.ts`: real SQLite/two-connection atomicity matrix.
- `package.json`: focused UoW test включён в общий test gate.

## Проверки

| Проверка | Результат |
| --- | --- |
| success: business + audit + idempotency + outbox | commit; оба connection видят `[1,1,1,1]` |
| fault after each of 4 boundaries | rollback; оба connection видят `[0,0,0,0]` |
| nested service with existing context | pass; outer rollback управляет записью |
| nested UoW/direct adapter transaction | `NESTED_TRANSACTION_FORBIDDEN` |
| `COMMIT`/`RELEASE`/`SAVEPOINT`, including comment/empty-statement bypasses | `TRANSACTION_CONTROL_FORBIDDEN`; no rows committed |
| captured `executeScript`/`close` in transaction | rejected; no partial commit |
| async callback with post-await write | sync reject, rollback, no `unhandledRejection` |
| escaped repository context | `TRANSACTION_CONTEXT_CLOSED` |
| focused database/UoW/policy tests + `npm run check` | pass |
| `npm run check && npm run test && npm run build` | pass |

## Review и риски

- Independent read-only review нашёл два bypass (ведущий `;` и async `unhandledRejection`);
  оба исправлены и закрыты regression.
- Final reviewer decision: `accepted`; blocker/major findings: none.
- Residual low: exotic callable function-thenable может дать generic transaction error;
  rollback сохраняется.
- Runtime всё ещё transitional `AppState`; production cutover запрещён.
