# DB-101 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: in-process SQLite transport; production cutover не выполнялся.

## Изменения

- `better-sqlite3` и declarations добавлены; `src/server/database.ts` реализует typed sync adapter.
- `store.ts`, migrations и converter tests больше не запускают `sqlite3` subprocess.
- Adapter errors redact SQL/params; Promise transaction callback запрещён; rollback и close проверены.
- Build копирует SQL migrations в `dist/migrations`; первый artifact smoke выявил этот gap, после исправления fresh DB startup прошёл.
- Architecture/runbook/project/execution docs обновлены.

## Проверки

| Проверка | Результат |
| --- | --- |
| `database.test.ts` | pass: open/query/execute/commit/rollback/async reject/redacted error/close |
| migrations + state migration | pass on isolated in-process DBs |
| поиск `spawnSync(sqlite3)` | none in `src/server` and `dist/index.js` |
| `npm run check` | pass |
| `npm test` | pass |
| `npm run build` | pass; migrations copied |
| isolated `dist/index.js` fresh DB | pass on darwin/arm64 Node 22.18; server reached listen on port 5184, then stopped |

## Риски

- Linux target не проверен и не является текущей personal-project target; перед Linux release нужен clean install/build/smoke.
- Connection PRAGMA/WAL/busy/schema/graceful shutdown относятся к `DB-102`.
- Domain runtime всё ещё использует global `AppState`; repositories/UoW впереди.

## Revalidation 2026-07-12

- Reviewer: orchestrator + read-only subagent.
- Решение: `changes_requested`.
- Negative probe: wrapper message redacted, но raw `error.cause.message` содержит SQL-derived secret.
- Registry status: `in_progress`; accepted evidence будет перезаписано после regression/full gate.

## Remediation acceptance 2026-07-12

- Changes: `DatabaseError` сохраняет только safe `causeCode`; raw cause не экспонируется.
- Regression: `database.test.ts` проверяет absent cause и JSON redaction.
- Exact leak probe: `leaked:false`, `cause` absent.
- `NODE_ENV=test npx tsx src/server/database.test.ts`: pass.
- `npm run check && npm run test && npm run build`: pass.
- Reviewer decision: `accepted`; registry status `done`.
