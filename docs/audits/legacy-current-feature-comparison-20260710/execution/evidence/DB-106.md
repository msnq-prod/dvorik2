# DB-106 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: explicit row mapping + actual v4 schema compatibility; repository implementations не входят.

## Изменения

- Shared mapper core: enum/NULL/integer/versioned JSON/UTC/local date/quantity diagnostics.
- Identity, catalog/stock, schedule, workflows/outbox и runtime mapper modules.
- 30 application tables имеют explicit round-trip coverage; schema-only `schema_migrations` исключён.
- Error содержит table/entity ID/field/code; corrupt payload не подменяется default-ом.

## Проверки

| Проверка | Результат |
| --- | --- |
| per-table round trips | pass |
| corrupt/v2/wrong-shape JSON with entity ID | rejected |
| unknown enum, NULL/composite ID, bad UTC/local date/time | covered |
| REAL/minor/unit mismatch and revision/version mismatch | rejected |
| outbox lease/status and attempt/max corrupt rows | rejected |
| actual v4 mapper-required columns | pass |
| state conversion + fresh/upgrade schema | pass |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent final review: `accepted`; reportable findings none.
- Residual: `shift.start < end` — domain/SCH invariant; timestamp write-validation style не везде унифицирован.
- Production cutover remains prohibited; registry status `done`.
