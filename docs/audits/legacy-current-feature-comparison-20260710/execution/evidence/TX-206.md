# TX-206 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: transactional schedule/swap services; runtime route cutover остаётся DB-108/109 scope.

## Изменения

- Day/shift/assignments используют `BEGIN IMMEDIATE`, exact versions, active/location/day/overlap validation и audit.
- Swap create/resolve повторно проверяют actor, users, shift/day/location/conflicts; accept атомарно меняет assignment, status, siblings, audit и inbox/outbox.
- Captured shift revision хранится в swap; stale preview сначала переводится в `expired`. Legacy pending swaps безопасно expire при v6/state conversion.
- Assignment diff сохраняет provenance неизменённых сотрудников.

## Проверки

| Проверка | Результат |
| --- | --- |
| invalid date/time/status/users/location/day/conflicts/versions | rejected |
| post-preview block/close/conflict/source removal/cancel/edit | rejected; stale rows expired |
| same-key replay / different payload | exact replay / `409` |
| concurrent accepts and concurrent edit/accept | one success; loser stale/replay |
| day/shift/assignment/swap/sibling/audit/notification/outbox/completion faults | full rollback |
| v5 upgrade and legacy state pending swaps | expired safely |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent review: `accepted`; final reportable findings none.
- Runtime endpoints remain legacy until DB-108/109 to prevent global AppState projection overwrite.
- Production cutover remains prohibited; registry status `done`.

