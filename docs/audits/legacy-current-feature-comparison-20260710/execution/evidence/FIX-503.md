# FIX-503 — evidence

- Дата/время: 2026-07-11, Asia/Vladivostok.
- Исполнитель/reviewer: ведущий агент; UI contract — read-only субагент; server audit завершён ведущим после auth failure субагента.
- Worktree: `gpt-version` untracked; production cutover не выполнялся.

## Изменения

- `src/shared/mediaUpload.ts`: единый decoded limit `5 МиБ = 5 242 880` байт, MIME allowlist, base64 byte-length без decode и UI validation.
- `src/server/index.ts`: endpoint-specific JSON parser, route pre-decode limit и JSON `413` для route/parser oversize; глобальный parser остаётся `2mb`.
- `src/client/src/pages/ProductsPage.tsx`: file picker JPEG/PNG/WebP, общий лимит/copy, client pre-read rejection, upload URL в form; bridge явно помечен временным до `MED-1101`.
- `src/shared/mediaUpload.test.ts`, `package.json`, OpenAPI summary, `PROJECT_STATE.md`, `execution/02_CURRENT_STATE.md` и `execution/03_DECISIONS.md` синхронизированы.

## Автоматические проверки

| Команда | Результат |
|---|---|
| `npm run check` | pass |
| `npm test` | pass, включая exact/+1 одинаковую base64 length и padding-derived bytes |
| `npm run build` | pass |

## HTTP boundary evidence

Изолированная среда: `PORT=5181`, отдельные `/tmp/fix503-state.json` и `/tmp/fix503-media`.

| Сценарий | Ожидание | Факт |
|---|---|---|
| Decoded ровно `5 242 880` байт | принять | `201`, `originalBytes=5242880` |
| Decoded `5 242 881` байт | route reject до decode/write | `413 MEDIA_TOO_LARGE` |
| JSON/base64 больше endpoint ingress | parser reject | `413 MEDIA_BODY_TOO_LARGE` |
| Filesystem effect | только exact создаёт файл | `storedFiles=1` |

## Browser evidence

- URL: `http://localhost:5181/`, in-app Browser.
- Flow: admin → Товары → Создать товар.
- File picker существует, `accept=image/jpeg,image/png,image/webp`.
- Видимы `Загрузить фото (до 5 МиБ)` и предупреждение про temporary JSON/`MED-1101`.
- Page identity/not blank/framework overlay/console health/interaction screenshot — pass; console warnings/errors отсутствуют.

## Риски

- Bridge всё ещё буферизует JSON/base64; production streaming, magic/decode/pixel limits относятся к `MED-1101`.
- Production local media fallback и обязательная compression capability остаются blockers `FIX-504`/`SEC-001`.

## Review

- Решение: `accepted`.
- Статус реестра обновлён: `yes`.
- Follow-up: unblock `DOC-002`, затем `SEC-001`.
