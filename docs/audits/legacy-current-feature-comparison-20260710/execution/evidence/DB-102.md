# DB-102 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: connection/readiness/shutdown policy; production cutover не выполнялся.

## Изменения

- `database.ts`: every connection enforces FK ON, WAL, busy timeout 5000ms and NORMAL synchronous; script policy restore and WAL checkpoint close.
- Exact schema versions, required runtime table set and rollback-only main-DB writable probe.
- `/ready` returns only safe status; API SIGINT/SIGTERM graceful close, CLI entrypoints explicitly close DB.
- `database-policy.test.ts`, package tests and architecture/runbook/project/execution docs updated.

## Проверки

| Проверка | Результат |
| --- | --- |
| two independent connections | FK=1, WAL, synchronous=1, configured busy timeout on both |
| orphan insert | rejected with `SQLITE_CONSTRAINT_FOREIGNKEY` |
| second writer | waits configured 200ms test policy then `SQLITE_BUSY` |
| readiness | exact schema pass; missing/future mismatch; readonly write probe unavailable |
| close/reopen | checkpoint close idempotent; integrity check `ok` |
| `npm run check`, `npm test`, `npm run build` | pass |
| production artifact SIGINT | policy/schema logged; shutdown logged; exit code `0` |

## Риски

- Readiness does not repair incompatible schema; migration command/startup must establish expected version first.
- No hidden writer retry is implemented; application conflict semantics remain later TX work.
- Production cutover remains prohibited.

## Revalidation 2026-07-12

- Reviewer: orchestrator + read-only subagent.
- Решение: `changes_requested`.
- Negative probe: DB с versions `[1,2,3]`, но без `products`, возвращает `ready:true`.
- Registry status: `pending`; accepted evidence будет перезаписано после structural-schema regression/full gate.

## Remediation acceptance 2026-07-12

- Changes: `checkDatabaseReadiness` требует expected table set; runtime передаёт все required tables.
- Negative probe: versions `[1,2,3]` без `products` → `SCHEMA_VERSION_MISMATCH`.
- Positive smoke: isolated fresh runtime DB → `ready:true`, schema `[1,2,3]`.
- `database-policy.test.ts`, `npm run check`, `npm run test`, `npm run build`: pass.
- Reviewer decision: `accepted`; registry status `done`.
