# DB-103 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: quantity representation/converter/schema projection; production cutover не выполнялся.

## Изменения

- `shared/quantity.ts`: scale 1000, unit precision, range, normalization and integer arithmetic.
- Stock, reversal, inventory, import, merge, reports, product threshold and state loading use the shared converter.
- Migration 003 adds integer minor columns, rejects invalid legacy data and enforces REAL/minor compatibility triggers.
- Mixed-unit merge is rejected before mutation; state migration emits/validates minor fields.
- Quantity ADR/project/execution docs and test chain updated.

## Проверки

| Проверка | Результат |
| --- | --- |
| converter boundaries | 0.001 accepted; fractional `шт`, >3 decimals, nonfinite, overflow rejected; large minor round-trip pass |
| drift | 1000 × 0.001 through converter and 1000 actual domain receipts both equal exactly 1 |
| migration guard | overprecision legacy value rolls back v3; version remains `[1,2]` |
| DB invariants | exact minor values; overprecision/unit/product-id trigger negatives pass |
| merge/report | mixed units rejected before mutation; report quantities normalized |
| `npm run check`, `npm test`, `npm run build` | pass |
| fresh artifact | schema versions `[1,2,3]`, startup pass, graceful exit `0` |

## Риски

- Human-number `AppState` and REAL mirrors remain transitional until DB-105/106 repository cutover; minor columns are canonical target, not yet sole runtime source.
- Rich import quantity syntax is deferred to `IMP-804`; mixed-unit UI totals to UI/product work.
- Production cutover remains prohibited.

## Revalidation 2026-07-12

- Reviewer: orchestrator + read-only subagent.
- Решение: `changes_requested`.
- Negative probes: migration v2→v3 и v3 trigger принимают `1.000001` как minor `1000`.
- Registry status: `pending`; accepted evidence будет перезаписано после migration/trigger regression/full gate.

## Remediation acceptance 2026-07-12

- Changes: SQL floating tolerance aligned with shared converter tolerance instead of fixed `0.01` scaled units.
- Migration negative: legacy `1.000001` rejected; schema versions remain `[1,2]`.
- Trigger negative: v3 insert `1.000001`/minor `1000` rejected.
- `migrations.test.ts`, `state-migration.test.ts`, `npm run check`, `npm run test`, `npm run build`: pass.
- Copied `dist/migrations/003_quantity_minor_units.sql` refreshed by build.
- Reviewer decision: `accepted`; registry status `done`.
