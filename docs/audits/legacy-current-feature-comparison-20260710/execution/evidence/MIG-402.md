# MIG-402 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Production cutover: не выполнялся.

## Изменения

- Migration `004_runtime_schema.sql`: migration batches, Telegram updates, WebApp notifications.
- Sessions: token hash/version/update time; shifts: persisted start/end; swaps/workflows: version/update time.
- Outbox: max attempts, lease owner/token/expiry, error code, failed time, version; consistency triggers.
- Legacy `processing` recovery в `pending` с explicit error code; converter сохраняет shift hours и timestamps.
- State notifications разделены на WebApp table и sent Telegram outbox.

## Проверки

| Проверка | Результат |
| --- | --- |
| fresh v2→v4 vs v3→v4 schema fingerprint | exact match |
| repeated migration | no pending migrations |
| legacy processing outbox | recovered; data preserved |
| max attempts/attempt overflow/lease/time/token hash/read-state negatives | rejected |
| non-default shift hours + v4 updated timestamps converter | preserved, non-epoch |
| runtime schema required-column smoke | pass |
| isolated readiness | `ready:true`, versions `[1,2,3,4]` |
| `npm run check && npm run test && npm run build` | pass |

## Review/риски

- Independent review нашёл shift/outbox/epoch converter gaps; все закрыты regression.
- Final decision: `accepted`; reportable findings none.
- Residual: upgrade preservation fixture samples representative altered rows, не every table.
- Registry status: `done`.
