# FIX-502 — evidence

- Дата/время: 2026-07-11, Asia/Vladivostok.
- Исполнитель/reviewer: ведущий агент; server и UI privacy traces — два read-only субагента.
- Worktree: `gpt-version` untracked; production cutover не выполнялся.

## Изменения

- `src/server/domain.ts`, `src/server/index.ts`: server-owned visibility для shifts/swaps; seller получает только own shifts, а seller swap view исключает foreign `fromShiftId`.
- `src/shared/types.ts`: `SellerShiftSwapRequest`.
- `src/client/src/pages/SchedulePage.tsx`: удалены оба fallback на `nextShifts[0]`/весь `shifts`; при отсутствии own scheduled shifts показывается «Нет своих смен для обмена» без selector/submit.
- `src/server/domain.test.ts`, `src/client/src/pages/SchedulePage.test.tsx`: privacy, no-mutation, own/foreign selector и empty-state regressions.
- OpenAPI summary и `PROJECT_STATE.md` синхронизированы.

## Автоматические проверки

| Команда | Результат |
|---|---|
| `npm run check` | pass |
| `npm test` | pass: включая domain privacy и schedule page tests |
| `npm run build` | pass |

## HTTP и UI negative evidence

Изолированная среда: `PORT=5180`, отдельный `DVORIK_STATE_FILE=/tmp/fix502-state.json`.

| Сценарий | Факт |
|---|---|
| Active seller без shifts: `GET /api/schedule` | `200`, `[]` |
| Direct `POST /api/schedule/swaps` с foreign `shift-2` | `403 FORBIDDEN`; swaps/outbox не изменились |
| Seller `GET /api/schedule/swaps` | foreign `fromShiftId` отсутствует |
| Admin `GET /api/schedule` | `200`, обе team shifts доступны |
| Browser seller → График → Обменяться | empty state видим; «Моя смена» selector отсутствует; foreign shift ID/comment отсутствуют; console warnings/errors отсутствуют |

## Риски

- Список активного staff и locations остаётся отдельным product/API contract; FIX-502 закрывает данные чужих shifts, не общий staff directory.
- Runtime persistence остаётся transitional; production cutover запрещён.

## Review

- Решение: `accepted`.
- Статус реестра обновлён: `yes`.
- Follow-up: `FIX-503`.
