# SEC-000 — evidence

- Дата: 2026-07-12, Asia/Vladivostok.
- Scope: dev/demo route hardening; production cutover не выполнялся.

## Изменения

- `src/server/index.ts`: `/api/auth/demo`, `/api/dev/config` и `/api/dev/telegram/init-data` регистрируются только при `DVORIK_DEV_TOOLS=1`; enabled routes ограничены loopback.
- При выключенном флаге reserved URLs явно возвращают JSON `404` до SPA fallback.
- Production/OpenAPI не рекламирует dev/demo paths.
- `src/client/src/main.tsx`: production UI больше не делает implicit demo login и не показывает user selector без dev config.
- `src/server/dev-tools.test.ts`, `package.json`, project/execution context обновлены.

## Проверки

| Проверка | Результат |
| --- | --- |
| `npm run check` | pass |
| `dev-tools.test.ts` | pass: known `u-super` demo POST, config и init-data дают `404`; cookie отсутствует; OpenAPI paths отсутствуют |
| `npm test` | pass |
| `npm run build` | pass |

## Риски

- Тест подтверждает disabled path и explicit config policy; remote-address rejection реализован отдельным loopback guard.
- Настоящие production auth/session semantics продолжаются в `SEC-002` и DB/TX blocks.
