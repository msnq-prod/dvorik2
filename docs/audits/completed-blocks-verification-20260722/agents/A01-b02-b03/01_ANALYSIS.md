# A01 — проверка B02/B03

Дата: 2026-07-22. Объект: `gpt-version`. Продуктовый код не изменялся.

## Вывод

- B02: частично подтверждён. Заявленный транспортный рефакторинг и базовые гарантии команд работают, но критерий B02 о модели данных для всего scope и его артефакты не закрыты.
- B03: ядро каталога и складских операций подтверждено. Полное завершение не доказано: нет подтверждения мобильного E2E/сканирования и часть поведения полок из плана не покрыта.

## Подтверждённое

### B02

- HTTP создаёт сервисы и UoW, а не выполняет бизнес-транзакции: `gpt-version/src/server/index.ts:167-208`.
- Статическая проверка запрещает mutation SQL и `sessionDatabase.transaction()` в HTTP transport: `gpt-version/src/server/transport-boundary.test.ts:5-10`.
- Идемпотентность, rollback business/audit/outbox и конкурентная обработка stock-команд покрыты: `stock-operation-service.test.ts:88-95,220-248`, `stock-operation-concurrency.test.ts:70-99`.
- В production legacy runtime отключён: `index.ts:50-68`.

### B03

- Миграция добавляет группы, производителей, тип товара, массу пачки, упаковки, неизменяемую историю цен, total inventory и placement projection: `migrations/009_catalog_warehouse.sql:1-116`.
- Ограничения БД требуют массу для весового товара, согласуют тип товара с группой и запрещают update/delete истории цен: тот же файл, `42-58,105-116`.
- Сервисный тест проверяет роли, группы, производителя, массу, упаковку, цену, barcode, replay и rollback: `catalog-service.test.ts:47-103`.
- Складской тест проверяет целые пачки/штуки, запрет коррекции продавцу, брак, идемпотентность и стабильную ошибку: `stock-operation-service.test.ts:78-120`.
- UI карточки товара содержит работу с группами, производителями, типом, массой, упаковками и ценой: `src/client/src/pages/ProductsPage.tsx:82-212,402-436`.

## Тестовый прогон

`npm test` в `gpt-version` завершился успешно 2026-07-22: прошли catalog, transport boundary, stock, concurrency, migrations и полный прочий набор.
