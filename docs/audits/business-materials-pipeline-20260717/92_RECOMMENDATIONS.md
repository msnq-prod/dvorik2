# Recommendations

## Immediate

1. Do not send the four current PPTX files to the client.
2. Preserve the run as immutable audit evidence and mark it unsuitable for use.
3. Do not attempt cosmetic repair of these decks.

## Rebuild foundation

4. Set the canonical mode map: `gpt-version` current, `recovered-dvorik` legacy,
   explicit authority for target docs, and explicit disposition of the root
   loyalty prototype.
5. Build a complete source manifest and class-based inventory for UI, API,
   Telegram, jobs, tables, integrations, permissions and flags.
6. Block Inventory Gate on any unmatched surface, local closure source,
   unsupported claim, unknown-covered dimension or empty mandatory map.
7. Require exact claim-level evidence and independent source reread.

## Learning

8. Require named roles and dispositions: teach current, reference legacy,
   preview target, technical only, defer, or exclude.
9. Transform claim meaning into user action, system check, data change, result
   and recovery; keep IDs only as hidden traceability metadata.
10. Replace generic troubleshooting with scenario, safe action, retry rule,
    owner, evidence and recovery check.
11. Block visual handoff on placeholders, missing roles or lifecycle ambiguity.

## Visual

12. Create a main client overview, verified process decks, role paths, Telegram
    explainer, database/data map, troubleshooting and quick references.
13. Use real screenshots for current flows, clearly labeled conceptual diagrams
    for target flows and explicit replacement guidance for legacy flows.
14. Keep lifecycle labels visible on every applicable slide.
15. Enforce source-copy comparison, duplicate detection, asset provenance,
    typography and required process dimensions.

## QA and orchestration

16. Separate author/compositor and reviewer actor/run identities.
17. Record actual model route and artifact hash for every review.
18. Add objective, semantic, usability and client-safety gates alongside schema
    integrity.
19. Propagate gap impact, not only gap IDs.
20. Keep `assert-terminal`, validate chronology and define artifact path bases.

## Acceptance

The replacement package is acceptable only when a nontechnical representative
of each actual role can identify lifecycle status, complete the normal flow,
explain the system/data result and choose the safe error action without coaching.

