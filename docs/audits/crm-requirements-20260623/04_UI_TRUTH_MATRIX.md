# UI truth matrix

| UI | Что показывает | Реальный источник | Совпадение |
|---|---|---|---|
| Дашборд | статистика лояльности | локальный `mockStats` | нет |
| Пользователи | список клиентов | локальный `mockUsers` | нет |
| Кассиры | список кассиров | локальный `mockCashiers` | нет |
| Рассылки | статусы и прогресс | локальный `mockBroadcasts` | нет |
| Кампании | статистика кампаний | локальный `mockCampaigns` | нет |
| Настройки | параметры системы | локальный React state | нет |
| Роли меню | доступ владельца | жёстко заданная роль `owner` | нет |

Целевые CRM-экраны отсутствуют.
